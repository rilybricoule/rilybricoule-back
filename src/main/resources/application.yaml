spring:
  application:
    name: RilyBricoule

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/rilybricoule?useSSL=false&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8000
